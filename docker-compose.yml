services:
  postgres:
    image: postgres:16-alpine
    container_name: electricity-postgres
    environment:
      POSTGRES_DB: electricity_trading
      POSTGRES_USER: electricity
      POSTGRES_PASSWORD: electricity_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: